# CLAUDE.md

Este workspace usa **CloneLab** (skill em `SKILL.md`).

- Defina aqui: `CLONE_HOME=<pasta do dataset> · CLONE_PKG=<caminho deste pacote>`
- Em toda sessão com trabalho de imagem, execute as 6 fases de `SKILL.md`
  (CONTRATO → IDENTIFICAR → ESCOLHER (código) → COMPOR → COPY → NEGATIVO/QC → ENTREGAR).
- Nunca entregue imagem sem o `qc.md` preenchido com veredito.
